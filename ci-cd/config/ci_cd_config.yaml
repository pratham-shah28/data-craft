# CI/CD Configuration
# Settings for model training pipeline automation

gcp:
  project_id: "datacraft-479223"  # Your GCP project ID
  region: "us-east1"  # Your GCP region
  dataset_id: "datacraft_ml"  # BigQuery dataset ID
  bucket_name: "model-datacraft-sanskar"  # GCS bucket name for storing model artifacts

notifications:
  email:
    smtp_server: "smtp.gmail.com"  # SMTP server address
    smtp_port: 587  # SMTP port (587 for TLS, 465 for SSL)
    from_email: "sharmasanskar360@gmail.com"  # Sender email
    to_email: "sharmasanskar004@gmail.com"  # Recipient email
    use_tls: true  # Use TLS encryption
    # Note: EMAIL_SMTP_PASSWORD must be set as GitHub secret

model_registry:
  base_path: "models"  # Path where CI/CD saves production models (with commit SHA)

rollback:
  enabled: true
  min_improvement_threshold: 0.0  # Minimum improvement in composite score to deploy

